# Copy this file to `.env` and fill in values.
# Do NOT commit your real secrets. `.env` is ignored.

# --- Azure/OpenAI credentials ---
# Preferred: Azure OpenAI
# AZURE_OPENAI_ENDPOINT=https://<your-azure-openai>.openai.azure.com/
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key

# Alternatively, OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your_openai_api_key

# Optional API version for Azure OpenAI (default: 2025-03-01-preview)
# AZURE_OPENAI_API_VERSION=2025-03-01-preview

# Default model name (used by CLI --model if not provided)
# AZURE_OPENAI_MODEL=gpt-5

# --- MCP (Model Context Protocol) ---
# Base URL to your MCP server
MCP_URL=http://localhost:7998/mcp/

# Optional auth header. Either "Key: Value" or "Key=Value"
# Example: MCP_AUTH_HEADER=Authorization: Bearer <token>
# MCP_AUTH_HEADER=

# --- Timeouts and behavior (seconds) ---
# Overall MCP request timeout
MCP_TIMEOUT=30

# Server-Sent Events read timeout
MCP_SSE_READ_TIMEOUT=300

# Per-tool-call timeout
MCP_TOOL_TIMEOUT=120

# Per-turn model response assembly timeout
RESPONSES_TURN_TIMEOUT=180

# Conversation control
MAX_TURNS=6

# Thread offloading limits (used by Azure client wrappers)
THREAD_LIMIT=8

# Verbose logs: set to 1/true/yes to enable
VERBOSE=0

